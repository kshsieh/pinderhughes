!!!
%html{lang: "en"}
  %head
    %meta{charset: "utf-8"}/
    %meta{name: "viewport", content: "width=device-width, initial-scale=1.0"}/
    %title= content_for?(:title) ? yield(:title) : "Samora Pinderhughes | Pianist"
    = stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true 
    = javascript_include_tag "vendor/modernizr", "data-turbolinks-track" => true  
    = csrf_meta_tags 
  %body
    .row.large-2.columns#sidebar= render 'layouts/sidebar'
    = yield 
    %p= debug(params) if Rails.env.development?
    = javascript_include_tag "application" 

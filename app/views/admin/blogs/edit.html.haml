%h1.row Update blog
.row
  = form_for([:admin, @blog],:html => { :method => :patch }) do |f|
    = f.text_field :title, placeholder: 'Title'
    = f.text_area :entry, placeholder: 'Entry', rows: 15, cols: 1
    = f.submit "Save Update", class: 'button radius'